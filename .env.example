# URL of the backend API this frontend talks to. Tarrs auto-injects
# the primary backend's sandbox URL (e.g. https://acme.dev.tarrs.io)
# at deploy time. For local dev, point at your dev backend.
NEXT_PUBLIC_API_URL=http://localhost:3000
